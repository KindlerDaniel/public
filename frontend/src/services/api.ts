/**
 * api.ts
 * Platzhalter für API-Funktionen.
 */

export const fetchData = async () => {
  // Beispielhafter API-Aufruf (hier nur als Dummy implementiert)
  return Promise.resolve({ data: 'Hello World' });
};
